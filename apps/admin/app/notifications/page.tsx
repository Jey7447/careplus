import { Bell, CalendarDays, ClipboardList, HeartPulse, LayoutDashboard, Settings, Stethoscope, Users } from 'lucide-react';
import Link from 'next/link';
import { redirect } from 'next/navigation';
import { createClient } from '../../lib/supabase/server';

const navigation = [
  ['Dashboard', '/', LayoutDashboard],
  ['Appointments', '/appointments', CalendarDays],
  ['Patients', '/patients', Users],
  ['Doctors', '/doctors', Stethoscope],
  ['Notifications', '/notifications', ClipboardList],
  ['Settings', '#', Settings],
] as const;

type SearchParams = Promise<{ search?: string; status?: string; channel?: string }>;
export const dynamic = 'force-dynamic';
function formatDate(value: string | null) { if (!value) return 'Not recorded'; return new Intl.DateTimeFormat('en-NG', { day: '2-digit', month: 'short', year: 'numeric', hour: '2-digit', minute: '2-digit', timeZone: 'Africa/Lagos' }).format(new Date(value)); }
export default async function NotificationsPage({ searchParams }: { searchParams: SearchParams }) {
  const supabase = await createClient();
  const { data: claimsData } = await supabase.auth.getClaims();
  if (!claimsData?.claims?.sub) redirect('/login');
  const { data: staffRecord, error: staffError } = await supabase.from('careplus_staff_users').select('id, role, active').eq('auth_user_id', claimsData.claims.sub).eq('active', true).eq('role', 'admin').maybeSingle();
  if (staffError || !staffRecord) redirect('/unauthorized');
  const params = await searchParams; const search = (params.search ?? '').trim().toLowerCase(); const status = ['Pending', 'Sent', 'Delivered', 'Failed'].includes(params.status ?? '') ? params.status! : 'All'; const channel = ['SMS', 'Email', 'WhatsApp'].includes(params.channel ?? '') ? params.channel! : 'All';
  const { data: notifications, error } = await supabase.from('notifications').select('notification_id, appointment_id, recipient, recipient_contact, channel, message, status, notification_type, sent_at, delivered_at').order('notification_id', { ascending: false }).limit(100);
  const rows = notifications ?? [];
  const filteredRows = rows.filter((item) => { if (status !== 'All' && item.status !== status) return false; if (channel !== 'All' && item.channel !== channel) return false; if (!search) return true; return `${item.recipient ?? ''} ${item.recipient_contact ?? ''} ${item.message ?? ''} ${item.notification_type ?? ''} ${item.appointment_id ?? ''}`.toLowerCase().includes(search); });
  const counts = { total: rows.length, pending: rows.filter((n) => n.status === 'Pending').length, sent: rows.filter((n) => n.status === 'Sent').length, delivered: rows.filter((n) => n.status === 'Delivered').length, failed: rows.filter((n) => n.status === 'Failed').length }; const email = typeof claimsData.claims.email === 'string' ? claimsData.claims.email : 'Authenticated staff';
  return <main className="min-h-screen"><div className="flex min-h-screen"><aside className="hidden w-64 border-r border-[var(--border)] bg-white lg:flex lg:flex-col"><div className="flex items-center gap-3 p-6"><div className="grid h-10 w-10 place-items-center rounded-xl bg-slate-900 text-white"><HeartPulse size={22} /></div><div><b>CarePlus</b><p className="text-xs text-slate-500">Medical Centre</p></div></div><nav className="flex-1 px-3">{navigation.map(([label, href, Icon]) => href === '#' ? <div key={label} className="mb-1 flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm text-slate-400"><Icon size={18}/>{label}</div> : <Link key={label} href={href} className={`mb-1 flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm ${label === 'Notifications' ? 'bg-slate-100 font-medium' : 'text-slate-600'}`}><Icon size={18}/>{label}</Link>)}</nav><div className="border-t border-[var(--border)] p-5 text-xs text-slate-500">CarePlus Administration</div></aside><section className="flex-1"><header className="border-b border-[var(--border)] bg-white px-6 py-5 lg:px-8"><div className="mx-auto flex max-w-7xl items-center justify-between gap-6"><div><p className="text-sm text-slate-500">CarePlus Medical Centre</p><h1 className="mt-1 text-2xl font-semibold">Notifications</h1><p className="mt-1 text-sm text-slate-500">Monitor notification status and delivery history.</p></div><div className="hidden text-right sm:block"><p className="text-xs text-slate-500">Signed in as</p><p className="mt-1 text-sm font-medium text-slate-800">{email}</p></div></div></header><div className="mx-auto max-w-7xl space-y-6 p-6 lg:p-8"><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-5">{[['Total',counts.total],['Pending',counts.pending],['Sent',counts.sent],['Delivered',counts.delivered],['Failed',counts.failed]].map(([label,value])=><div key={label} className="rounded-2xl border border-[var(--border)] bg-white p-5"><p className="text-sm text-slate-500">{label}</p><p className="mt-3 text-3xl font-semibold">{value}</p></div>)}</div><section className="rounded-2xl border border-[var(--border)] bg-white p-6"><form className="grid gap-4 xl:grid-cols-[1fr_190px_190px_auto]" method="get"><label><span className="text-sm font-medium text-slate-700">Search notifications</span><input name="search" defaultValue={params.search ?? ''} placeholder="Recipient, contact, type, message or appointment ID" className="mt-2 w-full rounded-xl border border-slate-300 px-3 py-2.5 text-sm outline-none focus:border-slate-900 focus:ring-2 focus:ring-slate-200"/></label><label><span className="text-sm font-medium text-slate-700">Status</span><select name="status" defaultValue={status} className="mt-2 w-full rounded-xl border border-slate-300 bg-white px-3 py-2.5 text-sm"><option>All</option><option>Pending</option><option>Sent</option><option>Delivered</option><option>Failed</option></select></label><label><span className="text-sm font-medium text-slate-700">Channel</span><select name="channel" defaultValue={channel} className="mt-2 w-full rounded-xl border border-slate-300 bg-white px-3 py-2.5 text-sm"><option>All</option><option>SMS</option><option>Email</option><option>WhatsApp</option></select></label><button type="submit" className="self-end rounded-xl bg-slate-900 px-5 py-2.5 text-sm font-medium text-white hover:bg-slate-800">Apply filters</button></form></section><section className="overflow-hidden rounded-2xl border border-[var(--border)] bg-white"><div className="flex items-center justify-between border-b border-slate-200 px-6 py-5"><div><h2 className="font-semibold">Notification history</h2><p className="mt-1 text-sm text-slate-500">Showing {filteredRows.length} of up to 100 loaded notifications.</p></div>{error&&<span className="rounded-full bg-red-50 px-3 py-1 text-xs text-red-700">Unable to load notifications</span>}</div><div className="overflow-x-auto"><table className="min-w-full text-sm"><thead className="bg-slate-50 text-left text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-6 py-3 font-medium">Notification</th><th className="px-6 py-3 font-medium">Recipient</th><th className="px-6 py-3 font-medium">Channel</th><th className="px-6 py-3 font-medium">Status</th><th className="px-6 py-3 font-medium">Sent</th><th className="px-6 py-3 font-medium">Delivered</th></tr></thead><tbody className="divide-y divide-slate-100">{filteredRows.map((item)=>{const href=`/notifications/${item.notification_id}`;return <tr key={item.notification_id} className="hover:bg-slate-50"><td className="p-0"><Link href={href} className="block max-w-xs px-6 py-4 outline-none focus:outline-none focus-visible:outline-none"><p className="font-medium text-slate-900">#{item.notification_id} · {item.notification_type??'Notification'}</p><p className="mt-1 truncate text-xs text-slate-500">{item.message??'No message recorded'}</p>{item.appointment_id&&<p className="mt-1 text-xs text-slate-500">Appointment #{item.appointment_id}</p>}</Link></td><td className="p-0"><Link href={href} className="block px-6 py-4 outline-none focus:outline-none focus-visible:outline-none"><p className="font-medium text-slate-800">{item.recipient??'Not recorded'}</p><p className="mt-1 text-xs text-slate-500">{item.recipient_contact??'Not recorded'}</p></Link></td><td className="p-0"><Link href={href} className="block px-6 py-4 outline-none focus:outline-none focus-visible:outline-none"><span className="inline-flex items-center gap-2 text-slate-700"><Bell size={14}/>{item.channel??'Not recorded'}</span></Link></td><td className="p-0"><Link href={href} className="block px-6 py-4 outline-none focus:outline-none focus-visible:outline-none"><span className={`rounded-full px-2.5 py-1 text-xs font-medium ${item.status==='Delivered'?'bg-emerald-50 text-emerald-700':item.status==='Failed'?'bg-red-50 text-red-700':item.status==='Sent'?'bg-slate-100 text-slate-700':'bg-amber-50 text-amber-700'}`}>{item.status??'Unknown'}</span></Link></td><td className="p-0"><Link href={href} className="block whitespace-nowrap px-6 py-4 text-slate-600 outline-none focus:outline-none focus-visible:outline-none">{formatDate(item.sent_at)}</Link></td><td className="p-0"><Link href={href} className="block whitespace-nowrap px-6 py-4 text-slate-600 outline-none focus:outline-none focus-visible:outline-none">{formatDate(item.delivered_at)}</Link></td></tr>})}</tbody></table></div>{filteredRows.length===0&&<div className="p-12 text-center text-sm text-slate-500">No notifications match the current filters.</div>}</section></div></section></div></main>;
}
