import { ArrowLeft, Bell, CalendarDays, ClipboardList, HeartPulse, LayoutDashboard, Mail, Phone, Settings, Stethoscope, Users } from 'lucide-react';
import Link from 'next/link';
import { notFound, redirect } from 'next/navigation';
import { createClient } from '../../../lib/supabase/server';

const navigation = [
  ['Dashboard', '/', LayoutDashboard],
  ['Appointments', '/appointments', CalendarDays],
  ['Patients', '/patients', Users],
  ['Doctors', '/doctors', Stethoscope],
  ['Notifications', '/notifications', ClipboardList],
  ['Settings', '/settings', Settings],
] as const;

type PageProps = { params: Promise<{ id: string }> };
function formatDateTime(value: string | null) { if (!value) return 'Not recorded'; return new Intl.DateTimeFormat('en-NG', { day: '2-digit', month: 'long', year: 'numeric', hour: '2-digit', minute: '2-digit', timeZone: 'Africa/Lagos' }).format(new Date(value)); }
function statusClass(status: string) { if (status === 'Delivered') return 'bg-emerald-50 text-emerald-700'; if (status === 'Failed') return 'bg-red-50 text-red-700'; if (status === 'Sent') return 'bg-slate-100 text-slate-700'; return 'bg-amber-50 text-amber-700'; }
export const dynamic = 'force-dynamic';
export default async function NotificationDetailsPage({ params }: PageProps) {
  const supabase = await createClient();
  const { data: claimsData } = await supabase.auth.getClaims();
  if (!claimsData?.claims?.sub) redirect('/login');
  const { data: staffRecord, error: staffError } = await supabase.from('careplus_staff_users').select('id, role, active').eq('auth_user_id', claimsData.claims.sub).eq('active', true).eq('role', 'admin').maybeSingle();
  if (staffError || !staffRecord) redirect('/unauthorized');
  const { id } = await params; const notificationId = Number(id); if (!Number.isSafeInteger(notificationId) || notificationId < 1) notFound();
  const { data: notification, error } = await supabase.from('notifications').select('notification_id, appointment_id, recipient, recipient_contact, channel, message, status, notification_type, sent_at, delivered_at, twilio_message_sid').eq('notification_id', notificationId).maybeSingle();
  if (error || !notification) notFound();
  const email = typeof claimsData.claims.email === 'string' ? claimsData.claims.email : 'Authenticated staff';
  return <main className="min-h-screen"><div className="flex min-h-screen"><aside className="hidden w-64 border-r border-[var(--border)] bg-white lg:flex lg:flex-col"><div className="flex items-center gap-3 p-6"><div className="grid h-10 w-10 place-items-center rounded-xl bg-slate-900 text-white"><HeartPulse size={22}/></div><div><b>CarePlus</b><p className="text-xs text-slate-500">Medical Centre</p></div></div><nav className="flex-1 px-3">{navigation.map(([label,href,Icon])=><Link key={label} href={href} className={`mb-1 flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm outline-none focus:outline-none focus-visible:outline-none ${label==='Notifications'?'bg-slate-100 font-medium text-slate-900':'text-slate-600 hover:bg-slate-50 hover:text-slate-900'}`}><Icon size={18}/>{label}</Link>)}</nav><div className="border-t border-[var(--border)] p-5 text-xs text-slate-500">CarePlus Administration</div></aside><section className="flex-1"><header className="border-b border-[var(--border)] bg-white px-6 py-5 lg:px-8"><div className="mx-auto flex max-w-7xl items-center justify-between gap-6"><div><Link href="/notifications" className="inline-flex items-center gap-2 text-sm text-slate-500 hover:text-slate-900"><ArrowLeft size={16}/> Back to notifications</Link><p className="mt-4 text-sm text-slate-500">CarePlus Medical Centre</p><h1 className="mt-1 text-2xl font-semibold">Notification #{notification.notification_id}</h1><p className="mt-1 text-sm text-slate-500">Review notification content, channel and delivery status.</p></div><div className="hidden text-right sm:block"><p className="text-xs text-slate-500">Signed in as</p><p className="mt-1 text-sm font-medium text-slate-800">{email}</p></div></div></header><div className="mx-auto max-w-5xl space-y-6 p-6 lg:p-8"><section className="rounded-2xl border border-[var(--border)] bg-white p-6"><div className="flex flex-col gap-5 md:flex-row md:items-start md:justify-between"><div><p className="text-xs font-medium uppercase tracking-wide text-slate-500">{notification.notification_type ?? 'Notification'}</p><h2 className="mt-2 text-xl font-semibold text-slate-900">{notification.recipient ?? 'Recipient not recorded'}</h2><p className="mt-1 text-sm text-slate-500">Notification #{notification.notification_id}</p></div><span className={`rounded-full px-3 py-1.5 text-xs font-medium ${statusClass(notification.status)}`}>{notification.status}</span></div></section><div className="grid gap-6 lg:grid-cols-2"><section className="rounded-2xl border border-[var(--border)] bg-white p-6"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-slate-100"><Bell size={19}/></div><div><h2 className="font-semibold">Delivery information</h2><p className="text-sm text-slate-500">Channel and recipient details</p></div></div><div className="mt-6 space-y-5 text-sm"><div><p className="text-xs text-slate-500">Channel</p><p className="mt-1 font-medium">{notification.channel ?? 'Not recorded'}</p></div><div><p className="text-xs text-slate-500">Recipient</p><p className="mt-1 font-medium">{notification.recipient ?? 'Not recorded'}</p></div><div><p className="text-xs text-slate-500">Contact</p><p className="mt-1 flex items-center gap-2 break-all"><Phone size={14}/>{notification.recipient_contact ?? 'Not recorded'}</p></div><div><p className="text-xs text-slate-500">Appointment</p>{notification.appointment_id ? <Link href={`/appointments/${notification.appointment_id}`} className="mt-1 inline-flex items-center gap-2 font-medium text-slate-800 hover:underline outline-none focus:outline-none focus-visible:outline-none"><CalendarDays size={14}/>Appointment #{notification.appointment_id}</Link> : <p className="mt-1">Not linked to an appointment</p>}</div></div></section><section className="rounded-2xl border border-[var(--border)] bg-white p-6"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-slate-100"><ClipboardList size={19}/></div><div><h2 className="font-semibold">Delivery timeline</h2><p className="text-sm text-slate-500">Recorded notification events</p></div></div><div className="mt-6 space-y-5 text-sm"><div><p className="text-xs text-slate-500">Sent</p><p className="mt-1">{formatDateTime(notification.sent_at)}</p></div><div><p className="text-xs text-slate-500">Delivered</p><p className="mt-1">{formatDateTime(notification.delivered_at)}</p></div><div><p className="text-xs text-slate-500">Twilio message SID</p><p className="mt-1 break-all font-mono text-xs">{notification.twilio_message_sid ?? 'Not recorded'}</p></div></div></section></div><section className="rounded-2xl border border-[var(--border)] bg-white p-6"><h2 className="font-semibold">Message</h2><div className="mt-5 rounded-xl bg-slate-50 p-5"><p className="whitespace-pre-wrap text-sm leading-7 text-slate-800">{notification.message ?? 'No message recorded.'}</p></div></section></div></section></div></main>;
}
