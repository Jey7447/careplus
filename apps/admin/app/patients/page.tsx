import { ArrowRight, CalendarDays, ClipboardList, HeartPulse, LayoutDashboard, Mail, Phone, Search, Settings, Stethoscope, Users } from 'lucide-react';
import Link from 'next/link';
import { redirect } from 'next/navigation';
import { createClient } from '../../lib/supabase/server';

const navigation = [['Dashboard','/dashboard',LayoutDashboard],['Appointments','/appointments',CalendarDays],['Patients','/patients',Users],['Doctors','/doctors',Stethoscope],['Notifications','/notifications',ClipboardList],['Settings','/settings',Settings]] as const;
type SearchParams = Promise<{ search?: string }>;
export const dynamic = 'force-dynamic';
function formatDate(date: string | null) { if (!date) return 'Not recorded'; return new Intl.DateTimeFormat('en-NG',{day:'2-digit',month:'short',year:'numeric',timeZone:'Africa/Lagos'}).format(new Date(`${date}T12:00:00+01:00`)); }
function formatCreatedAt(value: string | null) { if (!value) return 'Not recorded'; return new Intl.DateTimeFormat('en-NG',{day:'2-digit',month:'short',year:'numeric',timeZone:'Africa/Lagos'}).format(new Date(value)); }
export default async function PatientsPage({ searchParams }: { searchParams: SearchParams }) {
 const supabase=await createClient(); const {data:claimsData}=await supabase.auth.getClaims(); if(!claimsData?.claims?.sub) redirect('/login'); const {data:staffRecord}=await supabase.from('careplus_staff_users').select('id, role, active').eq('auth_user_id',claimsData.claims.sub).eq('active',true).eq('role','admin').maybeSingle(); if(!staffRecord) redirect('/unauthorized'); const params=await searchParams; const search=(params.search??'').trim().toLowerCase(); const {data:patients,error}=await supabase.from('patients').select('patient_id, first_name, last_name, phone_number, email, date_of_birth, gender, address, created_at').order('patient_id',{ascending:true}).limit(100); const rows=patients??[]; const filteredRows=search?rows.filter(p=>`${p.first_name} ${p.last_name} ${p.phone_number??''} ${p.email??''} ${p.gender??''} ${p.address??''}`.toLowerCase().includes(search)):rows; const withPhone=rows.filter(p=>Boolean(p.phone_number)).length; const withEmail=rows.filter(p=>Boolean(p.email)).length; const email=typeof claimsData.claims.email==='string'?claimsData.claims.email:'Authenticated staff';
 return <main className="min-h-screen bg-slate-100"><div className="flex min-h-screen"><aside className="hidden w-64 shrink-0 border-r border-slate-200 bg-white lg:flex lg:flex-col"><div className="flex items-center gap-3 p-6"><div className="grid h-11 w-11 place-items-center rounded-2xl bg-slate-950 text-white shadow-lg"><HeartPulse size={22}/></div><div><b className="text-sm text-slate-950">CarePlus</b><p className="text-xs text-slate-500">Medical Centre</p></div></div><nav className="flex-1 px-3">{navigation.map(([label,href,Icon])=><Link key={label} href={href} className={`group mb-1 flex items-center gap-3 rounded-xl px-3 py-3 text-sm transition-all ${label==='Patients'?'bg-slate-950 text-white shadow-lg shadow-slate-900/10':'text-slate-600 hover:bg-slate-100 hover:text-slate-950'}`}><Icon size={18}/><span>{label}</span>{label==='Patients'&&<ArrowRight size={15} className="ml-auto"/>}</Link>)}</nav><div className="border-t border-slate-200 p-5 text-xs text-slate-500">CarePlus Administration</div></aside><section className="min-w-0 flex-1"><header className="bg-slate-950 px-6 py-7 text-white lg:px-10"><div className="mx-auto flex max-w-7xl items-end justify-between gap-6"><div><p className="text-xs font-medium uppercase tracking-[0.18em] text-slate-400">CarePlus Medical Centre</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Patients</h1><p className="mt-2 text-sm text-slate-400">Review patient records, contact information and registration details.</p></div><div className="hidden text-right sm:block"><p className="text-xs text-slate-500">Signed in as</p><p className="mt-1 text-sm font-medium">{email}</p></div></div></header><div className="mx-auto max-w-7xl space-y-6 p-5 lg:p-8"><div className="grid gap-4 sm:grid-cols-3">{[['Total patients',rows.length],['With phone',withPhone],['With email',withEmail]].map(([label,value])=><div key={label} className="group rounded-2xl bg-slate-950 p-5 text-white shadow-sm transition-transform hover:-translate-y-0.5"><div className="flex items-center justify-between"><p className="text-sm text-slate-400">{label}</p><Users size={17} className="text-slate-500"/></div><p className="mt-5 text-3xl font-semibold tracking-tight">{value}</p><div className="mt-3 h-1 overflow-hidden rounded-full bg-slate-800"><div className="h-full w-1/2 rounded-full bg-slate-400 transition-all group-hover:w-3/4"/></div></div>)}</div><section className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200/70 lg:p-6"><div className="mb-4 flex items-center gap-2"><div className="grid h-9 w-9 place-items-center rounded-xl bg-slate-100"><Search size={17}/></div><div><h2 className="font-semibold text-slate-950">Find a patient</h2><p className="text-xs text-slate-500">Search by name, contact, gender or address.</p></div></div><form className="grid gap-4 lg:grid-cols-[1fr_auto]" method="get"><label><span className="sr-only">Search patients</span><input name="search" defaultValue={params.search??''} placeholder="Name, phone, email, gender or address" className="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm outline-none transition focus:border-slate-950 focus:bg-white focus:ring-4 focus:ring-slate-100"/></label><button type="submit" className="rounded-xl bg-slate-950 px-6 py-3 text-sm font-medium text-white transition hover:-translate-y-0.5 hover:bg-slate-800">Search patients</button></form></section><section className="overflow-hidden rounded-2xl bg-slate-950 text-white shadow-sm"><div className="flex items-center justify-between border-b border-slate-800 px-5 py-5 lg:px-6"><div><p className="text-xs font-medium uppercase tracking-[0.18em] text-slate-500">Patient records</p><h2 className="mt-1 font-semibold">Registered patients</h2><p className="mt-1 text-sm text-slate-400">Showing {filteredRows.length} of up to 100 loaded patients.</p></div>{error&&<span className="rounded-full border border-red-400/20 bg-red-400/10 px-3 py-1 text-xs text-red-300">Unable to load</span>}</div><div className="overflow-x-auto"><table className="min-w-full text-sm"><thead className="bg-slate-900 text-left text-xs uppercase tracking-wider text-slate-500"><tr><th className="px-6 py-4 font-medium">Patient</th><th className="px-6 py-4 font-medium">Contact</th><th className="px-6 py-4 font-medium">Date of birth</th><th className="px-6 py-4 font-medium">Gender</th><th className="px-6 py-4 font-medium">Registered</th></tr></thead><tbody className="divide-y divide-slate-800">{filteredRows.map(patient=>{const href=`/patients/${patient.patient_id}`;const linkClass='block px-6 py-4 outline-none';return <tr key={patient.patient_id} className="group transition hover:bg-slate-900"><td className="p-0"><Link href={href} className={linkClass}><div className="flex items-center gap-3"><div className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-slate-800 text-slate-300"><Users size={17}/></div><div><p className="font-medium">{patient.first_name} {patient.last_name}</p><p className="mt-1 text-xs text-slate-500">Patient #{patient.patient_id}</p></div></div></Link></td><td className="p-0"><Link href={href} className={linkClass}><p className="flex items-center gap-2 text-slate-300"><Phone size={14}/>{patient.phone_number??'Not recorded'}</p><p className="mt-1 flex items-center gap-2 break-all text-xs text-slate-500"><Mail size={14}/>{patient.email??'Not recorded'}</p></Link></td><td className="p-0"><Link href={href} className={linkClass+' whitespace-nowrap text-slate-300'}>{formatDate(patient.date_of_birth)}</Link></td><td className="p-0"><Link href={href} className={linkClass+' whitespace-nowrap text-slate-300'}>{patient.gender??'Not recorded'}</Link></td><td className="p-0"><Link href={href} className={linkClass+' whitespace-nowrap text-slate-400'}>{formatCreatedAt(patient.created_at)}</Link></td></tr>})}</tbody></table></div>{filteredRows.length===0&&<div className="p-12 text-center text-sm text-slate-500">No patients match the current search.</div>}</section></div></section></div></main>;
}
