import { ArrowRight, CalendarDays, ClipboardList, HeartPulse, LayoutDashboard, Mail, MapPin, Phone, Search, Settings, Stethoscope, Users } from 'lucide-react';
import Link from 'next/link';
import { redirect } from 'next/navigation';
import { createClient } from '../../lib/supabase/server';
const navigation=[['Dashboard','/dashboard',LayoutDashboard],['Appointments','/appointments',CalendarDays],['Patients','/patients',Users],['Doctors','/doctors',Stethoscope],['Notifications','/notifications',ClipboardList],['Settings','/settings',Settings]] as const;
type SearchParams=Promise<{search?:string;status?:string}>; export const dynamic='force-dynamic';
export default async function DoctorsPage({searchParams}:{searchParams:SearchParams}){const supabase=await createClient();const{data:claimsData}=await supabase.auth.getClaims();if(!claimsData?.claims?.sub)redirect('/login');const{data:staffRecord}=await supabase.from('careplus_staff_users').select('id, role, active').eq('auth_user_id',claimsData.claims.sub).eq('active',true).eq('role','admin').maybeSingle();if(!staffRecord)redirect('/unauthorized');const params=await searchParams;const search=(params.search??'').trim().toLowerCase();const status=params.status==='Inactive'?'Inactive':params.status==='Active'?'Active':'All';const{data:doctors,error}=await supabase.from('doctors').select('doctor_id, first_name, last_name, specialty, phone, email, active, branch_id').order('doctor_id',{ascending:true}).limit(100);const rows=doctors??[];const branchIds=[...new Set(rows.map(d=>d.branch_id).filter((id):id is number=>id!==null))];const{data:branches}=branchIds.length?await supabase.from('branches').select('*').in('branch_id',branchIds):{data:[]};const branchMap=new Map((branches??[]).map(branch=>{const r=branch as Record<string,unknown>;return[branch.branch_id,String(r.name??r.branch_name??'CarePlus branch')]}));const filteredRows=rows.filter(d=>{const s=d.active?'Active':'Inactive';if(status!=='All'&&s!==status)return false;if(!search)return true;return`${d.first_name} ${d.last_name} ${d.specialty??''} ${d.phone??''} ${d.email??''} ${branchMap.get(d.branch_id)??''}`.toLowerCase().includes(search)});const activeCount=rows.filter(d=>d.active).length;const inactiveCount=rows.filter(d=>!d.active).length;const specialties=new Set(rows.map(d=>d.specialty).filter(Boolean)).size;const email=typeof claimsData.claims.email==='string'?claimsData.claims.email:'Authenticated staff';return <main className="min-h-screen bg-slate-100"><div className="flex min-h-screen"><aside className="hidden w-64 shrink-0 border-r border-slate-200 bg-white lg:flex lg:flex-col"><div className="flex items-center gap-3 p-6"><div className="grid h-11 w-11 place-items-center rounded-2xl bg-slate-950 text-white shadow-lg"><HeartPulse size={22}/></div><div><b className="text-sm text-slate-950">CarePlus</b><p className="text-xs text-slate-500">Medical Centre</p></div></div><nav className="flex-1 px-3">{navigation.map(([label,href,Icon])=><Link key={label} href={href} className={`group mb-1 flex items-center gap-3 rounded-xl px-3 py-3 text-sm transition-all ${label==='Doctors'?'bg-slate-950 text-white shadow-lg shadow-slate-900/10':'text-slate-600 hover:bg-slate-100 hover:text-slate-950'}`}><Icon size={18}/><span>{label}</span>{label==='Doctors'&&<ArrowRight size={15} className="ml-auto"/>}</Link>)}</nav><div className="border-t border-slate-200 p-5 text-xs text-slate-500">CarePlus Administration</div></aside><section className="min-w-0 flex-1"><header className="bg-slate-950 px-6 py-7 text-white lg:px-10"><div className="mx-auto flex max-w-7xl items-end justify-between gap-6"><div><p className="text-xs font-medium uppercase tracking-[0.18em] text-slate-400">CarePlus Medical Centre</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Doctors</h1><p className="mt-2 text-sm text-slate-400">Review care providers, specialties, branches and availability.</p></div><div className="hidden text-right sm:block"><p className="text-xs text-slate-500">Signed in as</p><p className="mt-1 text-sm font-medium">{email}</p></div></div></header><div className="mx-auto max-w-7xl space-y-6 p-5 lg:p-8"><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[['Total doctors',rows.length],['Active',activeCount],['Inactive',inactiveCount],['Specialties',specialties]].map(([label,value])=><div key={label} className="group rounded-2xl bg-slate-950 p-5 text-white shadow-sm transition-transform hover:-translate-y-0.5"><div className="flex items-center justify-between"><p className="text-sm text-slate-400">{label}</p><Stethoscope size={17} className="text-slate-500"/></div><p className="mt-5 text-3xl font-semibold tracking-tight">{value}</p><div className="mt-3 h-1 overflow-hidden rounded-full bg-slate-800"><div className="h-full w-1/2 rounded-full bg-slate-400 transition-all group-hover:w-3/4"/></div></div>)}</div><section className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200/70 lg:p-6"><div className="mb-4 flex items-center gap-2"><div className="grid h-9 w-9 place-items-center rounded-xl bg-slate-100"><Search size={17}/></div><div><h2 className="font-semibold text-slate-950">Find a doctor</h2><p className="text-xs text-slate-500">Search providers by name, specialty, branch or contact.</p></div></div><form className="grid gap-4 lg:grid-cols-[1fr_220px_auto]" method="get"><label><span className="sr-only">Search doctors</span><input name="search" defaultValue={params.search??''} placeholder="Name, specialty, branch, phone or email" className="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm outline-none transition focus:border-slate-950 focus:bg-white focus:ring-4 focus:ring-slate-100"/></label><label><span className="sr-only">Status</span><select name="status" defaultValue={status} className="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm outline-none focus:border-slate-950 focus:ring-4 focus:ring-slate-100"><option>All</option><option>Active</option><option>Inactive</option></select></label><button type="submit" className="rounded-xl bg-slate-950 px-6 py-3 text-sm font-medium text-white transition hover:-translate-y-0.5 hover:bg-slate-800">Apply filters</button></form></section><section className="overflow-hidden rounded-2xl bg-slate-950 text-white shadow-sm"><div className="flex items-center justify-between border-b border-slate-800 px-5 py-5 lg:px-6"><div><p className="text-xs font-medium uppercase tracking-[0.18em] text-slate-500">Care providers</p><h2 className="mt-1 font-semibold">Doctor records</h2><p className="mt-1 text-sm text-slate-400">Showing {filteredRows.length} of up to 100 loaded doctors.</p></div>{error&&<span className="rounded-full border border-red-400/20 bg-red-400/10 px-3 py-1 text-xs text-red-300">Unable to load</span>}</div><div className="overflow-x-auto"><table className="min-w-full text-sm"><thead className="bg-slate-900 text-left text-xs uppercase tracking-wider text-slate-500"><tr><th className="px-6 py-4 font-medium">Doctor</th><th className="px-6 py-4 font-medium">Specialty</th><th className="px-6 py-4 font-medium">Branch</th><th className="px-6 py-4 font-medium">Contact</th><th className="px-6 py-4 font-medium">Status</th></tr></thead><tbody className="divide-y divide-slate-800">{filteredRows.map(d=>{const href=`/doctors/${d.doctor_id}`;const linkClass='block px-6 py-4 outline-none';return <tr key={d.doctor_id} className="group transition hover:bg-slate-900"><td className="p-0"><Link href={href} className={linkClass}><div className="flex items-center gap-3"><div className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-slate-800 text-slate-300"><Stethoscope size={17}/></div><div><p className="font-medium">Dr. {d.first_name} {d.last_name}</p><p className="mt-1 text-xs text-slate-500">Doctor #{d.doctor_id}</p></div></div></Link></td><td className="p-0"><Link href={href} className={linkClass+' font-medium text-slate-300'}>{d.specialty??'Not recorded'}</Link></td><td className="p-0"><Link href={href} className={linkClass}><p className="flex items-center gap-2 text-slate-300"><MapPin size={14}/>{branchMap.get(d.branch_id)??'Not recorded'}</p></Link></td><td className="p-0"><Link href={href} className={linkClass}><p className="flex items-center gap-2 text-slate-300"><Phone size={14}/>{d.phone??'Not recorded'}</p><p className="mt-1 flex items-center gap-2 break-all text-xs text-slate-500"><Mail size={14}/>{d.email??'Not recorded'}</p></Link></td><td className="p-0"><Link href={href} className={linkClass}><span className={`rounded-full border px-2.5 py-1 text-xs font-medium ${d.active?'border-emerald-400/20 bg-emerald-400/10 text-emerald-300':'border-slate-700 bg-slate-800 text-slate-400'}`}>{d.active?'Active':'Inactive'}</span></Link></td></tr>})}</tbody></table></div>{filteredRows.length===0&&<div className="p-12 text-center text-sm text-slate-500">No doctors match the current filters.</div>}</section></div></section></div></main>;
}
